#include <iostream>
#include <algorithm>
using namespace std;

string S;

int main(){

  cin >> S;
  
  int ans = 0;
  string a = "ZONe";
  for (int i = 0; i <= 8; ++i) {
      if (S.substr(i, 4) == a) {
          ans += 1;
      }
  }
  cout << ans << endl;
  return 0;
}
